---
import Heading from "./heading/heading";

interface Props {
  title: string;
  body: string;
  href?: string;
}

const { title, body } = Astro.props;
---

<div>
  <Heading level="h1" size="medium">{title}</Heading>
  <p>{body}</p>
</div>
